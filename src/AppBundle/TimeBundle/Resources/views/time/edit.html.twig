{% extends '::layout.html.twig' %}

{% block body -%}
    <h1>Arbeitszeit bearbeiten</h1>
    {{ form(edit_form) }}
    {{ form(delete_form) }}
    <a href="{{ path('time') }}">Alle anzeigen</a>
{% endblock %}
